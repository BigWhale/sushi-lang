# TEST_TYPE: runtime
# EXPECT_RUNTIME_EXIT: 0

# Test HashMap.keys() iterator with empty map
fn main() i32:
    let HashMap<i32, string> map = HashMap.new()

    # Iterate over keys (should not enter loop body)
    let i32 count = 0
    foreach(key in map.keys()):
        println("Unexpected: {key}")
        count := count + 1

    # Should not have iterated at all
    if (count == 0):
        return Result.Ok(0)

    return Result.Ok(1)
