# TEST_TYPE: runtime
# EXPECT_RUNTIME_EXIT: 0

# Test HashMap.keys() iterator with debug output
fn main() i32:
    let HashMap<string, i32> ages = HashMap.new()
    ages.insert("Alice", 30)
    ages.insert("Bob", 25)
    ages.insert("Charlie", 35)

    println("HashMap size: {ages.len()}")

    # Iterate over keys
    let i32 count = 0
    println("Starting foreach loop...")
    foreach(name in ages.keys()):
        println("Key: {name}")
        count := count + 1

    println("Count: {count}")

    # Should have iterated over all 3 entries
    if (count == 3):
        return Result.Ok(0)

    return Result.Ok(1)
