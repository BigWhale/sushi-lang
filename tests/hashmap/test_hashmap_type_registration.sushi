# Test that HashMap<K, V> is recognized as a valid generic type
# This test verifies type registration only (no methods needed yet)
fn main() i32:
    # Type should be recognized even though methods aren't implemented yet
    # We'll get errors about missing .new() method, but that's expected
    return Result.Ok(0)

struct Point:
    i32 x
    i32 y
