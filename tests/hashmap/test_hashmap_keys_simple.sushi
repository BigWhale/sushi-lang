# TEST_TYPE: runtime
# EXPECT_RUNTIME_EXIT: 0

# Test HashMap.keys() iterator with foreach loop
fn main() i32:
    let HashMap<string, i32> ages = HashMap.new()
    ages.insert("Alice", 30)
    ages.insert("Bob", 25)
    ages.insert("Charlie", 35)

    # Iterate over keys
    let i32 count = 0
    foreach(name in ages.keys()):
        print(name)
        count := count + 1

    # Should have iterated over all 3 entries
    if (count == 3):
        return Result.Ok(0)

    return Result.Ok(1)
