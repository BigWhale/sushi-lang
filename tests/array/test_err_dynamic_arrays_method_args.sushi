# Test dynamic array method argument mismatch
# EXPECT_ERROR_CODE: CE2006
fn main() i32:
  let i32[] numbers = new()

  # This should fail - push with wrong argument type
  numbers.push("string")

  return Result.Ok(0)
