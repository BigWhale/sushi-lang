# Ruling 4: a range yields i32, exactly as `foreach(i in 0..5)` does. Widening the range
# element type is separate work.
# EXPECT_ERROR_CODE: CE2002

fn main() i32:
    let i64[] a = from([0..5])
    println(a.len())
    return Result.Ok(0)
