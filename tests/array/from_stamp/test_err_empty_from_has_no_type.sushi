# An empty `from([])` in a position with no type has no element type to take. That is
# a user error at the literal, not the internal error CE0042 (#868).
# EXPECT_ERROR_CODE: CE2111

fn main() i32:
    println(from([]).len())
    return Result.Ok(0)
