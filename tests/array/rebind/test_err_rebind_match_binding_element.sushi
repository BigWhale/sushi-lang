# A match binding is a private deep copy, so a write through it is lost. CE2414.
# EXPECT_ERROR_CODE: CE2414

enum Holder:
    Items(i32[])

fn main() i32:
    let Holder h = Holder.Items(from([1, 2]))
    match h:
        Holder.Items(values) ->
            values[0] := 9
    return Result.Ok(0)
