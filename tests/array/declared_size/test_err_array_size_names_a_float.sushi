# EXPECT_ERROR_CODE: CE2099
# A constant that is not an integer cannot count elements.

const f64 WIDTH = 4.0

fn main() i32:
    let i32[WIDTH] counts = [1, 2, 3, 4]
    println("{counts.len()}")
    return Result.Ok(0)
