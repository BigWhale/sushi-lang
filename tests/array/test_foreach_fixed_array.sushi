# TEST_TYPE: runtime
# EXPECT_RUNTIME_EXIT: 0
# EXPECT_STDOUT_CONTAINS: 15

# Test foreach with fixed array
fn main() i32:
    let i32[5] arr = [1, 2, 3, 4, 5]
    let i32 sum = 0
    foreach(i32 x in arr.iter()):
        sum := sum + x
    print(sum)
    return Result.Ok(0)
