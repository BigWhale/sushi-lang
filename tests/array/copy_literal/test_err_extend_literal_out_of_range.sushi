# The stamp is real: a literal element that does not fit the receiver's element type is
# refused as the range fault it is, not as an `i32[]` argument (#576).
# EXPECT_ERROR_CODE: CE2073

fn main() i32:
    let u8[] b = from([1, 2])
    b.extend(from([300]))
    return Result.Ok(0)
