# The needle must be an element of the array's type.
# EXPECT_ERROR_CODE: CE2006
fn main() i32:
    let i32[] a = from([1, 2, 3])
    let bool found = a.contains("two")
    println("{found}")
    return Result.Ok(0)
