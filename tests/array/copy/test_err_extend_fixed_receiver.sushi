# A fixed array cannot grow: its length is part of its TYPE. `.push()` is refused for the
# same reason.
# EXPECT_ERROR_CODE: CE2023

fn main() i32:
    let i32[3] a = [1, 2, 3]
    let i32[] src = from([4, 5])
    a.extend(src)
    println(a[0])
    return Result.Ok(0)
