# Both arrays must hold the same element type.
# EXPECT_ERROR_CODE: CE2023

fn main() i32:
    let i32[] out = from([1, 2])
    let string[] words = from(["towel"])
    out.extend(words)
    println(out.len())
    return Result.Ok(0)
