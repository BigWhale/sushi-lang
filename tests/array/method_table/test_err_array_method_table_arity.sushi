# EXPECT_ERROR_CODE: CE2009
#
# capacity() takes no arguments. ONE arity rule answers for every row of the table.
fn main() i32:
    let i32[] a = from([1, 2, 3])
    let i32 room = a.capacity(1)
    println("{room}")
    return Result.Ok(0)
