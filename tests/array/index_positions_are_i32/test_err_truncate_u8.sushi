# The count of truncate is an i32 position (#870).
# EXPECT_ERROR_CODE: CE2006
# EXPECT_STDERR_CONTAINS: expected i32, got u8
fn main() i32:
    let i32[] a = from([1, 2, 3])
    let u8 n = 1 as u8
    a.truncate(n)
    println("{a.len()}")
    return Result.Ok(0)
