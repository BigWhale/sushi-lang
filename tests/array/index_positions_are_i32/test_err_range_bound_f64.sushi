# A float range bound is refused (#870). It was truncated to an integer.
# EXPECT_ERROR_CODE: CE2002
# EXPECT_STDERR_CONTAINS: cannot assign f64 to i32
fn main() i32:
    foreach(x in 0.5..3):
        println(x)
    return Result.Ok(0)
