# A fixed array literal of names as a method receiver takes the element type it infers,
# because the position has none (#889). It was the internal error CE0000.
# EXPECT_STDOUT_EXACT: "2\n"
# EXPECT_RUNTIME_EXIT: 0
# EXPECT_NO_LEAKS: true

fn main() i32:
    let string s = "mostly"
    println([s, s].len())
    return Result.Ok(0)
