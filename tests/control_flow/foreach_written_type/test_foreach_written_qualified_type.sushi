# The qualified spelling of the same position, behind an alias.
# EXPECT_STDOUT_EXACT: "7\n"
use "helpers/geo" as geo

fn main() i32:
    let geo.Vec[] vs = from([geo.Vec(3, 4)])
    foreach(geo.Vec v in vs.iter()):
        println("{v.x + v.y}")
    return Result.Ok(0)
