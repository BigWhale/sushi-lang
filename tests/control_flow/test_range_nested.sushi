# EXPECT_STDOUT_CONTAINS: Nested ranges:
# EXPECT_STDOUT_CONTAINS: 0,0
# EXPECT_STDOUT_CONTAINS: 2,2
fn main() i32:
    println("Nested ranges:")
    foreach(i in 0..3):
        foreach(j in 0..3):
            print(i)
            print(",")
            println(j)

    return Result.Ok(0)
