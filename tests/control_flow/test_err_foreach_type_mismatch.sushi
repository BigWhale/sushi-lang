# Test that foreach item type must match iterator element type
# EXPECT_ERROR_CODE: CE2034

fn main() i32:
    let i32[3] arr = [1, 2, 3]
    foreach(string s in arr.iter()):
        print(s)
    return Result.Ok(0)
