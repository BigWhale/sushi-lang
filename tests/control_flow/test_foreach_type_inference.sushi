# TEST_TYPE: runtime
# EXPECT_RUNTIME_EXIT: 0
# EXPECT_STDOUT_CONTAINS: 60

# Test foreach with type inference
fn main() i32:
    let i32[3] arr = [10, 20, 30]
    let i32 sum = 0
    foreach(x in arr.iter()):
        sum := sum + x
    print(sum)
    return Result.Ok(0)
