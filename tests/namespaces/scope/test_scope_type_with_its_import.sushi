# The same program with the import section 6.1 demands. The rule is Java's: to
# name a type, import the unit that declares it.
# EXPECT_STDOUT_EXACT: "3 4\n"
use "helpers/shapes"
use "helpers/geometry"

fn main() i32:
    let Vec v = origin().realise(Vec(0, 0))
    println("{v.x} {v.y}")
    return Result.Ok(0)
