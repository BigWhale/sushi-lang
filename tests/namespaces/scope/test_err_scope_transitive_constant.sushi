# The constant half of the same rule. A public constant of a unit this one did
# not import is not in scope, so the bare name declares nothing here.
# EXPECT_ERROR_CODE: CE1001
use "helpers/mid"

fn main() i32:
    println("{DEEP_ANSWER}")
    return Result.Ok(0)
