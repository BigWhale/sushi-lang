# An alias binds a name in the unit that wrote it, so the same alias twice is a
# collision (unit-namespaces.md section 8, CE3013).
# EXPECT_ERROR_CODE: CE3013
use "helpers/geo" as geo
use "helpers/own_math" as geo

fn main() i32:
    return Result.Ok(0)
