# One of two units that export one name. Nothing says which an unqualified
# `sine` means, so writing it bare is CE3012 and writing it behind an alias is
# not (unit-namespaces.md section 6).
public fn sine(f64 x) f64:
    return Result.Ok(x + 1.0)
