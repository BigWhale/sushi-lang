# An unknown type name in the error arm of a generic function with no instance is
# CE2001 at the error arm (#859).
# EXPECT_ERROR_CODE: CE2001
# EXPECT_STDERR_CONTAINS: test_err_template_unknown_error_arm.sushi:5:21: error [CE2001]: unknown type 'Absent'
fn g@(T)(T v) i32 | Absent:
    return Result.Ok(0)

fn main() i32:
    println("Mostly Harmless")
    return Result.Ok(0)
