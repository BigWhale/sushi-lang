# EXPECT_ERROR_CODE: CE2062
# EXPECT_STDERR_CONTAINS: generic 'Box' expects 1 type argument(s), got 2
# An extension template on a target with too many type parameters.
struct Box@(T):
    T v

extend Box@(T, U) twice() i32:
    return 2

fn main() i32:
    return Result.Ok(0)
