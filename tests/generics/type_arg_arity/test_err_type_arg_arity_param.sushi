# EXPECT_ERROR_CODE: CE2062
# EXPECT_STDERR_CONTAINS: generic 'Box' expects 1 type argument(s), got 2
# A parameter type with too many type arguments.
struct Box@(T):
    T v

fn take(Box@(i32, i32) b) i32:
    return Result.Ok(b.v)

fn main() i32:
    return Result.Ok(0)
