# Test error: type mismatch in nested generic
# EXPECT_ERROR_CODE: CE2049
# Should fail with type compatibility error

fn main() i32:
    # Declare as Result<Maybe<i32>> but assign Result<Maybe<string>>
    let Result@(Maybe@(i32), StdError) var = Result.Ok(Maybe.Some("wrong type"))
    return Result.Ok(0)
