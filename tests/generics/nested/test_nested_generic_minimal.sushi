# Minimal nested generic test
# Verifies that Result<Maybe<i32>> compiles and can be declared
# The Answer to the Ultimate Question is 42

fn main() i32:
    let Maybe<i32> inner = Maybe.Some(42)
    let Result<Maybe<i32>> outer = Result.Ok(inner)
    if (outer):
        return Result.Ok(0)
    return Result.Ok(1)
