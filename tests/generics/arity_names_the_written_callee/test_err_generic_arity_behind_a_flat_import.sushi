# A generic from another unit, named flat, keeps its written name in the arity error (#766).
# EXPECT_ERROR_CODE: CE2009
# EXPECT_STDERR_CONTAINS: 'pair'
use "helpers/arity_pair"

fn main() i32:
    let i32 n = pair@(i32)(1).realise(0)
    return Result.Ok(n)
