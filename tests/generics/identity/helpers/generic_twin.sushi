# A private generic of a name the consumer also declares. Each unit's call
# must reach its own body; the print marker says which body ran.
fn twin@(T)(nom T x) T:
    println("helper twin")
    return Result.Ok(x)

public fn twin_use() i32:
    return Result.Ok(twin(nom 1)??)
