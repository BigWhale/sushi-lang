# A match dispatches on an enum's variants or on an integer's value. A string has
# neither, so the scrutinee itself is refused.
# EXPECT_ERROR_CODE: CE2048

fn main() i32:
    let string s = "Mostly Harmless"
    match s:
        _ -> println("a")
    return Result.Ok(0)
