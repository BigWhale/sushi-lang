# The unit an alias binds. A static is as visible as its target type, so the public
# struct carries it with no marker of its own.
public struct Vec:
    i32 x
    i32 y

extend Vec static origin() Vec:
    return Vec(0, 0)
