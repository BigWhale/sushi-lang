# TEST_TYPE: compilation
# EXPECT_ERROR_CODE: CE2505
# Error test for an unhandled Result@(T) assignment: CE2505 names the fix, and it is
# now the only code the binding answers (#535).

fn get_value() i32:
    return Result.Ok(42)

fn main() i32:
    let i32 x = get_value()
    println(x)
    return Result.Ok(0)
