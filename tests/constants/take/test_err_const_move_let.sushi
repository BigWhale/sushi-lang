# The `let` twin: a binding straight from an owning constant would be a move, and a
# constant is never moved out of. Borrow it, or clone it.
# EXPECT_ERROR_CODE: CE2436

const string NAME = "Mostly Harmless"

fn main() i32:
    let string mine = NAME
    println(mine)
    return Result.Ok(0)
