# EXPECT_ERROR_CODE: CE0110
# Only an array constant can be indexed.

const i32 ANSWER = 42
const i32 NOT_AN_ELEMENT = ANSWER[0]

fn main() i32:
    println("{NOT_AN_ELEMENT}")
    return Result.Ok(0)
