# FFI: a match arm body is a block, so the gate reaches a `ptr` named in it.
# EXPECT_ERROR_CODE: CE5009
fn main() i32:
    match 0:
        0 ->
            let ptr p = 0
            println("Mostly Harmless")
        _ -> println("other")
    return Result.Ok(0)
