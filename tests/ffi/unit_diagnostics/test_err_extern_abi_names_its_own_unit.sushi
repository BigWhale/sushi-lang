# CE5003 fires in a second unit. The diagnostic must name that unit's file and line
# (#599).
# EXPECT_ERROR_CODE: CE5003
# EXPECT_STDERR_CONTAINS: bad_abi.sushi:3:20
use "helpers/bad_abi"

fn main() i32:
    println("Mostly Harmless")
    return Result.Ok(0)
