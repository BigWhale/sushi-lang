# main takes `string[] args` or no parameter. A parameter after `args` used to
# compile and was always 0.
# EXPECT_ERROR_CODE: CE0138
fn main(string[] args, i32 x) i32:
    println("n {args.len()} x {x}")
    return Result.Ok(0)
