# An unknown stdlib module must still fail compilation (guards the name-validation
# path now that collections/iter is a recognized bundled-source module).
use <collections/nope>

fn main() i32:
    return Result.Ok(0)
