# The same program on the INCREMENTAL path, which was already clean. It stays
# clean, and pins that the fix did not move the per-unit build (#493).
# EXPECT_STDOUT_EXACT: "Mostly Harmless\n3 2\n"
use <collections/strings>
use "helpers/counter"

fn main() i32:
    println("Mostly Harmless")

    let string a = "abc"
    let string b = "de"
    println("{a.len()} {width(b).realise(0)}")

    return Result.Ok(0)
