# TEST_TYPE: runtime
# EXPECT_RUNTIME_EXIT: 0

# Test HashMap.entries() on empty map

use <collections/hashmap>
fn main() i32:
    let HashMap<string, i32> map = HashMap.new()

    let i32 count = 0
    foreach(entry in map.entries()):
        print(entry.key)
        count := count + 1

    # Should have zero iterations
    if (count == 0):
        return Result.Ok(0)

    return Result.Ok(1)
