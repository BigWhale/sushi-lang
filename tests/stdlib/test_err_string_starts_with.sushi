use <collections/strings>

fn main() i32:
    # Error: starts_with() requires exactly 1 string argument
    let string s = "hello"
    let bool result = s.starts_with()
    println(result)
    return Result.Ok(0)
