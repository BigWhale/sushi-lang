# Diagnostic test: CE2509 with help message
# EXPECT_ERROR_CODE: CE2509
# Exercises: help message (no span)

fn main() i32:
    let string a = "hello"
    let string b = a + " world"
    return Result.Ok(0)
