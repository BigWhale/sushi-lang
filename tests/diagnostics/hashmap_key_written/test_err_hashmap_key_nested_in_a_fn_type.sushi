# An array key nested in a function type is refused at the written type (#773)
# EXPECT_ERROR_CODE: CE2058
# EXPECT_STDERR_CONTAINS: 6:10: error [CE2058]
use <collections/hashmap>

fn apply(fn(HashMap@(i32[], string)) -> i32 f) i32:
    return Result.Ok(0)

fn main() i32:
    return Result.Ok(0)
