# The same gate, reached through a different method of the same module (#501).
# EXPECT_ERROR_CODE: CE3015
# EXPECT_STDERR_CONTAINS: "string.split()"
use <collections/hashmap>

fn main() i32:
    let string s = "a,b"
    let string[] parts = s.split(",")
    println("{parts.len()}")
    return Result.Ok(0)
