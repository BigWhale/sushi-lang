# An argument is a value position.
# EXPECT_ERROR_CODE: CE2105
enum Color:
    Red

fn eat(i32 v) ~:
    println("{v}")
    return Result.Ok(~)

fn main() i32:
    eat(Color).realise(~)
    return Result.Ok(0)
