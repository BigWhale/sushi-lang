# Diagnostic test: CE2046 with text-only note
# EXPECT_ERROR_CODE: CE2046
# Exercises: note without span (predefined generic conflict)

enum Maybe:
    Something
    Nothing

fn main() i32:
    return Result.Ok(0)
