# CE2510: Mixed float type comparisons should fail
fn main() i32:
  let f32 x = 3.14 as f32
  let f64 y = 3.14

  # This should fail - mixed f32 and f64
  if (x < y):
    return Result.Ok(0)

  return Result.Ok(1)
