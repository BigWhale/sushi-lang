# CE2080: Unknown field name in named struct constructor

struct Point:
    i32 x
    i32 y

fn main() i32:
    # ERROR: 'z' is not a field of Point
    let Point p = Point(x: 10, y: 20, z: 30)
    return Result.Ok(0)
