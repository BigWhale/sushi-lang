# TEST_TYPE: runtime
# EXPECT_RUNTIME_EXIT: 0
# Explicit casts from int to float should pass
fn main() i32:
  let i32 x = 42
  let f64 y = 3.14

  # Cast int to float for comparison
  if ((x as f64) > y):
    # Cast int to float for arithmetic
    let f64 result = (x as f64) + y
    if (result > 45.0 and result < 46.0):
      return Result.Ok(0)

  return Result.Ok(1)
