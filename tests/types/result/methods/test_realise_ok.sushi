# EXPECT_RUNTIME_EXIT: 0
# EXPECT_STDOUT_CONTAINS: PASS: realise on Ok returns value
# EXPECT_STDERR_EMPTY: true

# Tests realise method on Ok variant

enum MyError:
    Error

fn get_value() Result<i32, MyError>:
    return Result.Ok(42)

fn main() i32:
    let i32 value = get_value().realise(0)
    if (value == 42):
        println("PASS: realise on Ok returns value")
        return Result.Ok(0)
    else:
        println("FAIL: realise returned wrong value")
        return Result.Ok(1)
