# EXPECT_RUNTIME_EXIT: 0
# EXPECT_STDOUT_CONTAINS: PASS: expect on Ok returns value
# EXPECT_STDERR_EMPTY: true

# Tests expect method on Ok variant

enum MyError:
    Error

fn get_value() i32 | MyError:
    return Result.Ok(42)

fn main() i32:
    let i32 value = get_value().expect("Expected a value")
    if (value == 42):
        println("PASS: expect on Ok returns value")
        return Result.Ok(0)
    else:
        println("FAIL: expect returned wrong value")
        return Result.Ok(1)
