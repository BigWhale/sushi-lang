# CE2510: Mixed integer type equality should fail
fn main() i32:
  let i16 x = 10 as i16
  let i32 y = 10

  # This should fail - mixed i16 and i32
  if (x == y):
    return Result.Ok(0)

  return Result.Ok(1)
