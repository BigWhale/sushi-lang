# CE2510: Mixed signed/unsigned type operations should fail
fn main() i32:
  let i32 signed = 42
  let u32 unsigned = 42 as u32

  # This should fail - mixed i32 and u32
  if (signed == unsigned):
    return Result.Ok(0)

  return Result.Ok(1)
