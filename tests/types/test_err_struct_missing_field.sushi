# CE2082: Missing required field in named struct constructor
# EXPECT_ERROR_CODE: CE2082

struct Point:
    i32 x
    i32 y

fn main() i32:
    # ERROR: Missing field 'y'
    let Point p = Point(x: 10)
    return Result.Ok(0)
