# CE2081: Duplicate field name in named struct constructor

struct Point:
    i32 x
    i32 y

fn main() i32:
    # ERROR: 'x' appears twice
    let Point p = Point(x: 10, y: 20, x: 15)
    return Result.Ok(0)
