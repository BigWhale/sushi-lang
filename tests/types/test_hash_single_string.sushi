# EXPECT_STDOUT_EXACT: "String hash = 10718936469131102457\n"
# EXPECT_RUNTIME_EXIT: 0
# Test hashing a single string (not in an array)

fn main() i32:
    let string s = "Mostly"
    let u64 hash = s.hash()
    println("String hash = {hash}")
    return Result.Ok(0)
