# The assignment target is the same rule, and it reached the same backend site (#661).
# EXPECT_ERROR_CODE: CE2106
# EXPECT_STDERR_CONTAINS: 'i32[]' has no field
fn main() i32:
    let i32[] rows = from([1, 2, 3])
    rows.len := 9
    println("{rows[0]}")
    return Result.Ok(0)
