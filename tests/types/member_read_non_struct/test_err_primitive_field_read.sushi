# A primitive holds no field. The fault is the position, and the diagnostic points
# at it (#661).
# EXPECT_ERROR_CODE: CE2106
# EXPECT_STDERR_CONTAINS: 'i32' has no field 'nope'
fn main() i32:
    let i32 n = 42
    let i32 v = n.nope
    println("{v}")
    return Result.Ok(0)
