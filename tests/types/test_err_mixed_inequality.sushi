# CE2510: Mixed type inequality should fail
fn main() i32:
  let f32 x = 5.5 as f32
  let f64 y = 10.0

  # This should fail - mixed f32 and f64
  if (x != y):
    return Result.Ok(0)

  return Result.Ok(1)
