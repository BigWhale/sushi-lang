# EXPECT_ERROR_CODE: CE2006
#
# f64.from_bits expects a u64; passing a u32 is an argument-type mismatch.
fn main() i32:
    let u32 narrow = 5 as u32
    let f64 x = f64.from_bits(narrow)
    println("{x}")
    return Result.Ok(0)
