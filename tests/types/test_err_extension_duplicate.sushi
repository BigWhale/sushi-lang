# Duplicate extension method error test
# EXPECT_ERROR_CODE: CE0101
# Should fail compilation with duplicate extension method error

extend i32 add(i32 value) i32:
  return Result.Ok(self + value)

# Duplicate extension method - should error
extend i32 add(i32 value) i32:
  return Result.Ok(self + value + 1)

fn main() i32:
  let i32 x = 5
  let i32 result = x.add(3)
  return Result.Ok(result)
