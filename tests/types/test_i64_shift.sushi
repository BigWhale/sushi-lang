# TEST_TYPE: runtime
# EXPECT_RUNTIME_EXIT: 0
# EXPECT_STDOUT_CONTAINS: 34359738368

# Shift on an i64 operand stays 64-bit (issue #84): 1 << 35.
fn main() i32:
    let i64 one = 1 as i64
    let i64 shifted = one << 35
    println(shifted)
    return Result.Ok(0)
