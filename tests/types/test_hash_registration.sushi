# Test that basic struct hashing still works (should pass even before we wire up Pass 1.8)

struct Point:
    i32 x
    i32 y

fn main() i32:
    let Point p = Point(10, 20)
    # This should work if old Pass 0 registration is still active
    let u64 h = p.hash()
    println("Point hash: {h}")
    return Result.Ok(0)
