# CE2510: Mixed type <= comparison should fail
fn main() i32:
  let u16 x = 100 as u16
  let u32 y = 200 as u32

  # This should fail - mixed u16 and u32
  if (x <= y):
    return Result.Ok(0)

  return Result.Ok(1)
