# Test that calling .realise() on Result<~> is an error
# EXPECT_ERROR_CODE: CE2506


fn do_nothing() ~:
  return Result.Ok(~)

fn main() i32:
  let Result<~, StdError> result = do_nothing()
  result.realise(~)  # Error: CE2506
  return Result.Ok(0)
