# A BINARY library's public struct, public enum with a payload, public perk with its
# concrete implementations, constrained generic function and kept private helper all
# reach the consumer through the manifest: the registry arms and the template arms of
# the `libraries` step (#675).
# EXPECT_RUNTIME_EXIT: 0
# EXPECT_STDOUT_EXACT: "box calm 8\ntin loud 3\n30\n7\n"
# EXPECT_NO_LEAKS
use <lib/binary_api_lib>

fn main() i32:
    let Crate a = Crate(7, "box")
    let Crate b = Crate(3, "tin")
    println(describe(a, Mood.Calm).realise("?"))
    println(describe(b, Mood.Loud(3)).realise("?"))
    let i32 two = 2
    let i32 three = 3
    println(heavier(nom two, nom three).realise(0).weigh())
    println(heavier(nom a, nom b).realise(Crate(0, "none")).weight)
    return Result.Ok(0)
