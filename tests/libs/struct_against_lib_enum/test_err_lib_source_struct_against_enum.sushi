# A consumer's struct `Color` beside a source library's public enum `Color` is CE0006
# at the consumer's declaration. The library's own code is not measured against the
# consumer's struct (the pytest gate tests/unit/test_struct_after_enum_is_refused.py
# pins the complete code list).
# EXPECT_ERROR_CODE: CE0006
use <lib/types_lib>

struct Color:
    i32 m

fn main() i32:
    return Result.Ok(0)
