# The contract unit. A .slib stores a perk's SOURCE TEXT and re-parses it at the
# consumer, so the '| E' channel has to survive that round trip (probe P8).
public enum GadgetError:
    OutOfRange

public perk Fallible:
    fn probe(i32 n) i32 | GadgetError
