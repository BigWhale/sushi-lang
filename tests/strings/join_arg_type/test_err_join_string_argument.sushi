# string.join takes a string[]; a plain string argument is refused at the argument.
# EXPECT_ERROR_CODE: CE2006
# EXPECT_STDERR_CONTAINS: expected string[], got string
use <collections/strings>

fn main() i32:
    let string sep = ", "
    println(sep.join("a"))
    return Result.Ok(0)
