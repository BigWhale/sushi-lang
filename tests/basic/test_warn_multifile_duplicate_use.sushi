# Test duplicate use statement warning
# EXPECT_ERROR_CODE: CW3001
use "helpers/math_utils"
use "helpers/foo_module"
use "helpers/math_utils"

fn main() i32:
    let Result<i32, StdError> result = add(10, 20)
    return Result.Ok(result.realise(0))
