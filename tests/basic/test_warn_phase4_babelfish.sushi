# TEST_TYPE: compilation
# EXPECT_ERROR_CODE: CW1001
# Test with different variable name


fn main() i32:
    let Result@(i32, StdError) babelfish = Result.Ok(42)
    let i32 x = babelfish.realise(0)
    return Result.Ok(0)
