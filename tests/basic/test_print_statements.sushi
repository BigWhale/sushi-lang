# EXPECT_RUNTIME_EXIT: 0
# TEST_TYPE: runtime
fn main() i32:
  let i32 x = 42
  let bool flag = true
  let string msg = "Hello"

  print(x)
  print(flag)
  print(msg)

  # Print expressions
  print(x + 10)
  print(not flag)

  # All print statements should work without crashing
  return Result.Ok(0)
