# An enum that holds itself by value has no finite size. The finite-types pass
# reports it as CE2095, the struct rule, since #677 (CE2052 is retired).
# EXPECT_ERROR_CODE: CE2095
enum BadList:
    Cons(i32, BadList)
    Nil()

fn main() i32:
    return Result.Ok(0)
