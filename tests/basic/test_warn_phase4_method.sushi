# TEST_TYPE: compilation
# EXPECT_ERROR_CODE: CW1001
# Test using the variable in a method call


fn main() i32:
    # Declare a variable with Result<i32> type
    let Result@(i32, StdError) result = Result.Ok(42)

    # Try to call a method on it
    let i32 x = result.realise(0)

    return Result.Ok(0)
