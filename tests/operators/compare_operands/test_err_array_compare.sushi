# EXPECT_ERROR_CODE: CE2514
# A dynamic array has no order.

fn main() i32:
    let i32[] a = from([1, 2])
    let i32[] b = from([1, 3])
    if (a < b):
        return Result.Ok(1)
    return Result.Ok(0)
