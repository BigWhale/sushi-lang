# EXPECT_ERROR_CODE: CE2518
# A dynamic array is a descriptor. Dividing two of them reached LLVM and failed
# the IR parse.

fn main() i32:
    let i32[] a = from([1, 2])
    let i32[] b = from([3, 4])
    let i32[] c = a / b
    println("{c.len()}")
    return Result.Ok(0)
