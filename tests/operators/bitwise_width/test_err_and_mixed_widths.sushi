# EXPECT_ERROR_CODE: CE2510
# `&` must refuse two widths, the way `+` already does. The backend would make
# them agree on its own, without saying so.

fn main() i32:
    let u8 mask = 0x0F
    let u32 value = 0x1234
    let u32 kept = mask & value
    println("{kept}")
    return Result.Ok(0)
