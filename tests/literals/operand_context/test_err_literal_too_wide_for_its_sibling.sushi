# A literal that does not fit the type of the other operand is out of range for
# THAT type.
# EXPECT_ERROR_CODE: CE2073
# EXPECT_STDERR_CONTAINS: "literal 300 out of range for u8"
fn main() i32:
    let u8 b = 1
    if (b == 300):
        println("never")
    return Result.Ok(0)
