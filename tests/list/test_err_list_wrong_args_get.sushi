# Test error: get() with wrong number of arguments (expects 1)
fn run_test() i32:
    let List<i32> data = List.new()
    # ERROR: get() expects exactly 1 argument (index)
    let i32 val = data.get()??
    return Result.Ok(0)

fn main() i32:
    let i32 result = run_test().realise(99)
    return Result.Ok(result)
