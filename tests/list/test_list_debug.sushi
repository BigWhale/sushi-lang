# EXPECT_STDOUT_CONTAINS: List<i32> {
# EXPECT_STDOUT_CONTAINS: len: 3, capacity: 4
# EXPECT_STDOUT_CONTAINS: [1] 20
fn main() i32:
    let List<i32> numbers = List.new()

    # Push some values
    numbers.push(10)
    numbers.push(20)
    numbers.push(30)

    println("=== List Debug ===")
    numbers.debug()

    return Result.Ok(0)
