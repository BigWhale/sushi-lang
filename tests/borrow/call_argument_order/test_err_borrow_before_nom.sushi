# A `let`-borrow of a slot of `a` is an argument of the same call that takes `nom a`. The
# callee owns `a` and may free it while it reads the borrow (#920).
# EXPECT_ERROR_CODE: CE2412

use <collections/strings>

fn keep(string v, nom string[] dst) i32:
    return Result.Ok(v.len() + dst.len())

fn main() i32:
    let string s = "abc"
    let string[] a = from([s.upper(), s.lower()])
    let string first = a[0]
    println(keep(first, nom a).realise(0))
    return Result.Ok(0)
