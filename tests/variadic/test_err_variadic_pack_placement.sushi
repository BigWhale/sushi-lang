# A type-pack TYPE-param must be the last type parameter -> CE0117.
# EXPECT_ERROR_CODE: CE0117
perk Display:
    fn display() string

fn f<...Ts: Display, U>(U first, ...Ts rest) ~:
    return Result.Ok(~)

fn main() i32:
    return Result.Ok(0)
