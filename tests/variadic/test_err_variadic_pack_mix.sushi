# A type-pack value-param cannot be combined with a v1 native variadic -> CE0118.
# EXPECT_ERROR_CODE: CE0118
perk Display:
    fn display() string

fn f<...Ts: Display>(...Ts rest, ...i32 nums) ~:
    return Result.Ok(~)

fn main() i32:
    return Result.Ok(0)
