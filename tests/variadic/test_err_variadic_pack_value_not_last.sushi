# A type-pack VALUE-param must be the last parameter -> CE0117.
# EXPECT_ERROR_CODE: CE0117
perk Display:
    fn display() string

fn f<...Ts: Display>(...Ts rest, i32 last) ~:
    return Result.Ok(~)

fn main() i32:
    return Result.Ok(0)
