# The contract's PARAMETER position, the twin of the return one (#667). The walk
# over a unit's declared type positions already yields it; the pass ignored it.
# EXPECT_ERROR_CODE: CE2001

perk Sink:
    fn put(peek self, NoSuchParam v) i32

fn main() i32:
    println("Mostly Harmless")
    return Result.Ok(0)
