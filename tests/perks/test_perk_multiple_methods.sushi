# tests/test_perk_multiple_methods.sushi
# Perk with multiple methods

perk Comparable:
    fn equals(&poke i32 other) bool
    fn less_than(&poke i32 other) bool

fn main() i32:
    return Result.Ok(0)
