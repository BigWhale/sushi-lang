# The same hole with a FUNCTION behind the alias (#733).
# EXPECT_ERROR_CODE: CE4003
# EXPECT_STDERR_CONTAINS: unknown perk: k.twice
use "helpers/kinds" as k

fn use_it@(T: k.twice)(T x) i32:
    return Result.Ok(x.volume())

fn main() i32:
    println("Mostly Harmless")
    return Result.Ok(0)
