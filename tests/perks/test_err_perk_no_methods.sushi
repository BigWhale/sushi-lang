# Error: a perk with no methods has an empty body and fails to parse.
#
# EXPECT_ERROR_CODE: CE6001
# EXPECT_STDERR_CONTAINS: unexpected token 'fn'

perk Empty:

fn main() i32:
    return Result.Ok(0)
