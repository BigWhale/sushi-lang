# Test: one declaration documented twice, from above and from inside its body.
# EXPECT_ERROR_CODE: CE7006

##: Documents the function from above. :##
fn probe() i32:
    ##: And documents the same function from inside its body. :##
    return Result.Ok(7)

fn main() i32:
    let i32 n = probe()??
    println("{n}")
    return Result.Ok(0)
