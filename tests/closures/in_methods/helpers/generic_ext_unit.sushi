public struct Crate@(T):
    T value

extend Crate@(T) doubled_len() i32:
    let i32 two = 2
    let fn(i32) -> i32 f = |i32 x| x * two
    let i32 r = f(self.measure()).realise(0)
    return r

extend Crate@(i32) measure() i32:
    return self.value
