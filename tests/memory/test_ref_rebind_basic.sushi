# EXPECT_STDOUT_EXACT: "6\n"
# EXPECT_RUNTIME_EXIT: 0
# EXPECT_NO_LEAKS: true
fn increment(&poke i32 counter) ~:
    counter := counter + 1
    return Result.Ok(~)

fn main() i32:
    let i32 x = 5
    increment(&poke x)
    println(x)
    return Result.Ok(0)
