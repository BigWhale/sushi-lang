# Test Own<Own<T>> - nested owned pointers
fn main() i32:
    let Own<i32> inner = Own.alloc(42)
    let Own<Own<i32>> outer = Own.alloc(inner)
    let Own<i32> unwrapped = outer.get()
    let i32 value = unwrapped.get()
    println(value)
    let i32 expected = 42
    return Result.Ok(value - expected)
