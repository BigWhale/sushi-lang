# EXPECT_STDOUT_EXACT: "Mostly Harmless\n"
# EXPECT_RUNTIME_EXIT: 0
# Test Own<string> - heap-allocated strings
fn main() i32:
    let Own<string> s = Own.alloc("Mostly Harmless")
    let string value = s.get()
    println(value)
    return Result.Ok(0)
