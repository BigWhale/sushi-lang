fn swap(&poke i32 a, &poke i32 b) ~:
    let i32 temp = a
    a := b
    b := temp
    return Result.Ok(~)

fn main() i32:
    let i32 x = 10
    let i32 y = 20
    swap(&poke x, &poke y)
    println(x)
    println(y)
    return Result.Ok(0)
