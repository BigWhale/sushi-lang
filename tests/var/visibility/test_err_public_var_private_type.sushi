# The leak fence: a public unit variable may not hand out a private type (CE3009),
# the rule a public constant already answers to.
# EXPECT_ERROR_CODE: CE3009

struct Secret:
    i32 n

public var Secret vault = Secret(1)

fn main() i32:
    println("{vault.n}")
    return Result.Ok(0)
