# The alias changes nothing about a constant: `poke t.STEP` has no address to take.
# EXPECT_ERROR_CODE: CE2400

use "helpers/tally" as t

fn bump(poke i32 n) ~:
    n := n + 1
    return Result.Ok(~)

fn main() i32:
    bump(poke t.STEP)
    return Result.Ok(0)
