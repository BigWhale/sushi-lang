# A lambda literal captures a local by value into a heap environment. Calling
# through the function value reads the captured copy back.
fn main() i32:
    let i32 n = 5
    let fn(i32) -> i32 f = |i32 x| x + n
    println(f(10).realise(0))    # 15
    return Result.Ok(0)
